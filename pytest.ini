[pytest]
testpaths = engine/tests
# core/ and session.py import as top-level modules, so engine/ goes on the path rather than
# sprinkling sys.path hacks through conftest.
pythonpath = engine
markers =
    slow: generates multi-GB files to exercise staging and the grid's height-cap scroll mapping
