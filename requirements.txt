# Fully pinned on purpose. Unpinned floors (`duckdb>=1.0`) mean a surprise upstream release can
# change CSV sniffing or SUMMARIZE's output columns with nothing in this repo to explain it.
# Verified together on Python 3.12.13, 2026-08-07.
#
# duckdb 1.5.5 specifics this code depends on — re-run engine/tests before bumping:
#   * sniff_csv reports an absent quote/escape/comment as the literal string '(empty)'
#   * reject_scans()/reject_errors() do NOT exist; bad rows are found via TRY_CAST instead
#   * delta_scan honors tombstones; time travel is `version => n` (not `AT (VERSION => n)`)
#   * read_xlsx takes `sheet =>` (not `sheet_name`)
duckdb==1.5.5
fastapi==0.141.1
uvicorn==0.52.1
python-multipart==0.0.32
# Cheap sheet enumeration for the .xlsx picker (load_workbook(read_only=True) gives dimensions
# without parsing cells). DuckDB's read_xlsx can read a named sheet but cannot list them.
openpyxl==3.1.5
# REQUIRED, not optional: without pytz, fetching any TIMESTAMP WITH TIME ZONE value raises
# "Invalid Input Error: Required module 'pytz' failed to import". DuckDB does not vendor it.
pytz==2026.3.post1
